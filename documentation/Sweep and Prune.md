Алгоритм **Sweep and Prune** (иногда называют **Sort and Sweep**) — это один из классических методов для **Broad Phase Collision Detection** (широкофазного обнаружения коллизий). Он используется, чтобы **быстро и эффективно находить потенциально пересекающиеся объекты** в 3D или 2D пространстве.

---

## 🔤 Перевод:

- **Sweep** — "прочёсывание", "сканирование"
    
- **Prune** — "обрезать", "отсечь"
    
- **Sweep and Prune** = "Прочёсывание и отсечение"
    

👉 Цель: **отсеять пары объектов, которые точно не столкнутся**, и оставить только те, которые могут пересечься, чтобы потом их точно проверить в **Narrow Phase**.

---

## 📦 Идея алгоритма:

Каждый объект оборачивается в **простую ограничивающую форму** — **AABB (Axis-Aligned Bounding Box)**.  
AABB — это короб, который **не поворачивается**, а остаётся выровненным по осям X, Y, Z.

Вместо того чтобы проверять **все пары объектов** (что даёт O(n²) сложность), алгоритм **использует сортировку по одной оси**, чтобы **быстро определить потенциальные пересечения**.

---

## 📐 Принцип работы (поэтапно):

---

### ✅ 1. **Выбор оси (или нескольких осей)**

Обычно берётся одна из координатных осей:

- В 2D: X и Y
    
- В 3D: X, Y и Z
    

Можно выбрать:

- **Только одну ось** (чаще X) — быстро, но менее точно.
    
- **Несколько осей** — точнее, но медленнее.
    

---

### ✅ 2. **Извлечение начала и конца интервалов**

Каждый объект представлен интервалом:

- **Start** (начало AABB по выбранной оси)
    
- **End** (конец AABB по той же оси)
    

Например, если у объекта A:

- `minX = 2`, `maxX = 5`
    

То интервал по X: `[2, 5]`

---

### ✅ 3. **Сортировка интервалов по начальной точке (Start)**

Список всех AABB **сортируется по minX** (или min по выбранной оси).

💡 Это даёт **упорядоченный список**, который можно "прочёсывать" для поиска перекрытий.

---

### ✅ 4. **Прочёсывание (Sweep)**

Идём по отсортированному списку:

- Для каждого AABB, сравниваем его с **последующими**, пока `start2 < end1` (пока есть потенциальное перекрытие).
    
- Если `start2 > end1`, больше проверять не нужно — дальше все объекты **уже отсортированы** и не пересекаются.
    

📌 Таким образом, алгоритм **пропускает большинство неподходящих пар**.

---

### ✅ 5. **Формирование списка потенциальных столкновений**

Каждая пара, у которой **перекрываются интервалы**, заносится в список **"может столкнуться"** (candidate pairs).

Позже эти пары идут в **Narrow Phase**.

---

## 🔄 Пример:

Допустим у нас 3 объекта с интервалами по X:

|Объект|minX|maxX|
|---|---|---|
|A|1|4|
|B|3|5|
|C|6|8|
### Сортировка:

- A: (1, 4)
    
- B: (3, 5)
    
- C: (6, 8)
    

### Sweep:

- A и B → перекрытие (3 < 4) → возможно столкновение
    
- B и C → нет перекрытия (6 > 5) → отбрасываем
    

👉 Результат: потенциальная пара **A–B**

---

## 💡 Преимущества:

|Преимущество|Объяснение|
|---|---|
|✅ Быстрее, чем полный перебор|Особенно, если объекты распределены равномерно.|
|✅ Простая реализация|Использует стандартную сортировку и линейный проход.|
|✅ Эффективен при небольших движениях|Если объекты мало двигаются, можно использовать **insertion sort**, т.к. порядок почти не меняется.|
## ❌ Недостатки:

|Недостаток|Пояснение|
|---|---|
|❌ Хуже работает при большой динамике|Если объекты быстро и хаотично движутся, эффективность падает.|
|❌ Зависим от оси|Если объекты перекрываются по одной оси, но не по другим — могут быть **ложные срабатывания**.|
|❌ Сложнее обрабатывать поворот|Работает только с AABB — не учитывает вращение.|
## 📈 Оптимизации:

- **Multi-axis sweep** — проверка по двум или трём осям, чтобы уменьшить ложные пересечения.
    
- **Temporal coherence** — сортировка с учётом движения во времени (если объекты почти не двигаются, сортировка быстрая).
    
- **Bounding Volume Updates** — при движении объектов можно обновлять их интервалы вместо перестройки.
    

---

## 📊 Сравнение с другими методами Broad Phase:

|Алгоритм|Особенности|
|---|---|
|**Sweep and Prune**|Эффективен при медленных изменениях. Работает на AABB.|
|**Spatial Hashing**|Делит пространство на ячейки. Быстро, но грубее.|
|**BVH (Bounding Volume Hierarchy)**|Иерархия объёмов. Лучше подходит для сложных объектов (много треугольников).|
|**Grid-based**|Пространство разбивается на сетку. Хорошо работает при равномерном распределении.|

Sweep and Prune